// Using the findTotal function to display a calculation

var number1 = 1000; // Change this value as needed
var number2 = 66;   // Change this value as needed
var number3 = 34;   // New variable added
var result;
var findTotal;

findTotal = function () {
    result = number1 + number2 + number3; // Updated to add three numbers
};

findTotal();

console.log(number1 + " + " + number2 + " + " + number3 + " = " + result);
